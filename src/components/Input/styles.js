import { StyleSheet } from "react-native";

export default StyleSheet.create({
  inputContainer: {
    marginBottom: 12,
    width: "100%"
  },
  input: {
    paddingHorizontal: 10,
    paddingVertical: 0,
    height: 30,
    width: "100%"
  }
});
